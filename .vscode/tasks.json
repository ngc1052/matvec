{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug build",
            "command": "cmake build -DCMAKE_BUILD_TYPE=Debug && cmake --build build",
            "type": "shell",
            "group": "build"
        },
        {
            "label": "Release build",
            "command": "cmake build -DCMAKE_BUILD_TYPE=Release && cmake --build build ",
            "type": "shell",
            "group": "build"
        }
    ]
}
